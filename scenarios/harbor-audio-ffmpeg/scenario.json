{
  "description": "liquidsoap encodes one playlist to mp3 and AAC (ADTS) through %ffmpeg and serves every listener itself through output.harbor with one shared encoder per mount.",
  "processes": [
    {
      "name": "liquidsoap",
      "server": true,
      "prepare": [["${LIQUIDSOAP}", "--cache-only", "main.liq"]],
      "cmd": ["${LIQUIDSOAP}", "main.liq"]
    }
  ],
  "mounts": [
    {"path": "/mp3", "content_type": "audio/mpeg", "nominal_kbps": 128},
    {"path": "/aac", "content_type": "audio/aac", "nominal_kbps": 128}
  ]
}
